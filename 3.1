#include <stdio.h>
#include <stdlib.h>
#include <errno.h>
#include <signal.h>

int main(void)
{
void sigint_handler(int sig);
char s[200];

if (signal(SIGNINT,sigint_handler) == SIG_ERR){
perror("signal");
exit(1);
}
printf("enter a string:\n");
if(fgets(s, 200, stdin)== NULL)
perror("gets");
else 
prinf("you entered: %s\n",s);

return 0;
}
void sigint_handler(int sig)
{
printf("Jangan kacau saya! \n");
}
